# ============================================================
# ReturnGuard 根 .gitignore
# 覆盖运行期产物与密钥，避免误提交（尤其 docker/.env 含数据库密码）
# ============================================================

# 密钥 / 环境变量（含密码，绝不能入库）
.env
.env.*
!.env.example

# Python 缓存
__pycache__/
*.pyc

# 工具链缓存与测试产物（此前未忽略，已在仓库工作区堆积）
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
.coverage.*
coverage.xml
htmlcov/

# 日志
*.log

# 本地数据库（运行时由 init_db 从 cases.json 重建）
*.db
*.db-wal
*.db-shm
*.sqlite3

# 本地调试产物（注意：运行期上传目录 uploads/ 由 demo/.gitignore 管理，保留 .gitkeep）
test_*.png
resp.json

# pytest-cov 临时覆盖文件（COVERAGE_FILE 重定向产物，非源码）
.cov_tmp
.cov_tmp*

# 编辑器 / 系统
.idea/
.vscode/
.DS_Store

# ============================================================
# 真实数据集原始文件（约 540MB，绝不入库）
# 仅作为 convert_datasets.py 的输入源，本地保留即可；
# 转换产物 demo/cases.json（真实数据种子，735KB）已入库，demo 直接跑它。
# 数据集获取方式见 demo/convert_datasets.py 文件头 docstring。
# ============================================================
Dataset/

# ReturnGuard 退件法医 · 评审一页纸

> 一句话定位：**把每一笔跨境退货纠纷，变成可决策的市场情报与可提交平台的举证材料。**
> 形态：FastAPI + ArkUI 风格大屏看板（本地可跑，演示态零外网、零费用）。

---

## 一、痛点（为什么做）

| 痛点 | 现状代价 |
|---|---|
| **退了还输** | 跨境卖家纠纷维权胜诉率普遍偏低（Demo 数据 **31%**，每 10 笔只赢 3 笔），七成退款"白亏"。 |
| **看不清根因** | 退货数据散在平台后台，缺聚合：不知道亏在哪些品类/供应商/地区/季节。 |
| **取证门槛高** | 单案要自己比对同款、截图框瑕疵、写举证文案，非技术卖家难上手。 |
| **AI 不可控** | 许多"智能"工具提示词藏在代码里，结果不可审计、不可复现。 |

---

## 二、方案（做了什么）

**左：给老板看的洞察看板**（市场洞察，12 卡 3×4）——自动聚合：
- ① 品类热力 ② 退货根因 ③ 供应商红黑榜 ④ 平台维权难度 ⑤ 暴增预警 ⑥ 烧钱 SKU
- ⑦ 洞察总结 ⑧ 选品建议 ⑩ 平台×供应商交叉
- **⑬ 退货地区分布 ⑭ 退货季节趋势 ⑮ 退货成本 & 供应商黑名单**（方向2 维度扩展，同一屏多维度）

**右：给运营用的取证工具**
- ⑨ 单案取证：上传「退货图 + 主图」→ 相似度 / 是否同款 / 缺陷红框（带置信度示意）/ 自动举证材料 / 可播放语音。
- ⑪ 平台适配举证包：按 Amazon/AliExpress/Temu/SHEIN 列出必备材料（只列客观要求，不替代平台裁决）。
- ⑫ 数据录入：手动补真实案件，双源物理隔离。

**工程底座**
- **双源物理隔离**：`cases.db`（演示种子）/ `cases_real.db`（真实空库），`?source=demo|real` 透传，演示数据永不污染真实库。
- **写接口鉴权**：`ANALYZE_API_KEY` 开启后 `POST/DELETE /api/cases`、`POST /api/analyze` 强制 Key（X-API-Key 或 `?key=`），演示态免鉴权。
- **Live / Mock 双模**：默认 mock（确定性、可复现、零外网）；切「真实 AI」走阿里云百炼 Token Plan 专属网关（OpenAI 兼容，qwen3.7-max），失败自动回退 mock 保证演示不中断。
- **提示词集中管理**：所有 AI 提示词抽至 `demo/prompts.py`，按功能 ①~⑥ + 编排层分组，可审计、可调参。

---

## 三、数据验证（不是 PPT，是真跑出来的）

- **真实数据回流**：`seed_real.py` 用标准库 urllib 调运行服务，灌入 **24 条**真实跨境纠纷（S3 蓝牙耳机电池鼓包近期 +150% 暴增为主故事线）。
  - mock 洞察正确产出异常预警（SKU-RG07 +150%）、供应商红黑榜（S3/S4/S5）、选品建议。
  - **live 真 LLM 归因通过**：根因 / 报告 / 4 条建议均为真实模型产出。
- **维度扩展实测**（demo 源 673 笔）：地区 欧洲 454 笔 / 北美 174 / 南美 44；季节 夏 500 笔 ≈ 春 2.9 倍；退货总成本 **¥141,708**（退款 + 物流 ≈¥20,873）；自动黑名单 **7 家**质量分<50 且带可解释理由。
- **单案取证实测**：mock 模式相似度 ≈78%、判定「疑似调包/非同款」、缺陷红框 + 举证材料 + 语音均可生成。

---

## 四、演示路径（录屏看板，约 4 分钟）

| 镜 | 时间 | 看什么 | 一句话 |
|---|---|---|---|
| 1 开场 | 0:00–0:18 | 四 KPI 全景 | 退货情报站定位 |
| 2 胜诉率 | 0:18–1:00 | 环形图 31% | 每 10 笔只赢 3 笔 |
| 3 红黑榜 | 1:00–1:50 | 供应商透视 + 下钻 | 哪些供应商最该换 |
| 4 维度卡（新） | 1:50–2:40 | ⑬⑭⑮ 同屏 | 地区/季节/成本+黑名单自动生成 |
| 5 单案取证 | 2:40–3:30 | 上传两图→举证 | 一笔退货变可提交材料 |
| 6 收尾+彩蛋 | 3:30–4:00 | 大屏 + prompts.py | 从单案到策略；AI 可审计 |

> 详细逐镜与真实数字见 `复赛录屏脚本_V2_维度扩展版.md` 与 `演示走位卡.md`（V2.0）。

---

## 五、差异化亮点（评委凭什么给高分）

1. **只取证不裁决**：前端明确标注代理指标、置信度为示意值，举证包只列平台客观要求——守住合规边界，不替平台做结论。
2. **可复现 + 可审计**：mock 确定性、live 可回退、提示词集中单一文件，演示稳定、评审可查。
3. **数据隔离严谨**：双 SQLite 物理隔离 + 写接口鉴权，真实业务数据不泄露、不被演示污染。
4. **维度真正可决策**：地区/季节/成本/黑名单不是花图表，而是直接指向"换哪家供应商、哪季提前备货"。

---

## 六、风险与边界（主动交底）

- **视觉/OCR/向量/重排**能力当前百炼 Token Plan 网关团队版未全量开通，统一回退 mock（结果确定性、可演示）；开通后改 `models_router.py` 内模型名即可无缝切换。
- **红框为示意**：当前为按缺陷类型生成的演示框，接入真实视觉模型后自动替换为检测坐标与真实分数。
- **live 模式需 Key + 公网图**：演示建议用 mock 保节奏，展示真 AI 提前 60s 预热。

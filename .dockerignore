# ReturnGuard 构建上下文忽略（仓库根；build context = ..）
# 仅用于减少 docker build 发送到守护进程的内容，不影响 COPY 逻辑。
# 注意：不要排除 docker/，否则 COPY docker/entrypoint.sh 会失败。

# 版本控制
.git
**/.git

# Python 缓存
**/__pycache__
**/*.pyc

# 文档（不进镜像）
*.md

# 本地数据库与密钥（运行时由 init_db 从 cases.json 重建，.env 由用户本地提供）
demo/cases.db
demo/.env
*.db
*.sqlite3

# 本地调试产物
demo/uploads/*
!demo/uploads/.gitkeep
test_*.png
resp.json

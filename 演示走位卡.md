# ReturnGuard 演示走位卡 · 逐镜映射真实页面 · V2.0

> **用途**：配合《复赛录屏脚本_V2_维度扩展版.md》使用——把每一条口播/镜头，映射到真实页面里"点哪里、看什么、出现什么数字"，避免现场找不着、口播与画面脱节。
> **版本**：V2.0（2026-08-19，对齐当前 demo 源 mock 数据；新增 ⑬⑭⑮ 维度扩展卡 + 提示词集中管理映射）
> **演示地址**：`http://localhost:8000`（服务已含本次前端打磨：取证三态步骤条 / 分析中提示 / 结果模式徽标 / 一键复制举证材料）

---

## 〇、开录前 5 分钟核对（照做一遍）

1. 服务已启动、页面 F11 全屏、顶部停在「市场洞察」tab；顶部数据源保持 **演示数据（demo）**。
2. **对照下方「高光场景速查表」核数字**：胜诉率 31%、SKU-303 +733%、SKU-306 +383%、S6/S3 黑榜、⑬欧洲454笔、⑭夏500笔、⑮总成本¥141,708/黑名单7家——若数字不同，说明数据源/种子变了，口播数字随之改，别硬念。
3. 桌面备好两张图：`退货图.png`、`主图.png`（单案取证用；没有就用 `demo/test_a.png` / `demo/test_b.png` 顶着）。
4. （彩蛋用）编辑器提前打开 `demo/prompts.py`，录到彩蛋镜时 Alt+Tab 切过去展示 1–2 秒。
5. 光标高亮 + 系统免打扰；关微信/邮件弹窗。

---

## 一、逐镜走位（时间轴 = 录屏脚本 V2）

### 镜 1 · 开场（0:00–0:18）｜市场洞察 tab · 四 KPI 卡
| 项 | 内容 |
|---|---|
| **操作** | 顶部点 `[市场洞察]`（`[data-tab="dash"]`）→ 鼠标从 KPI 卡左到右横扫，最后停在「维权胜诉率」环形图 |
| **预期画面** | 四张 KPI 卡：已分析退货 **673** / 累计退款 **¥120,835** / 维权胜诉率 **31%**（绿色弧形）/ 货不对板嫌疑率；无加载残留 |
| **口播要点** | 一屏看懂全局；结尾停在胜诉率卡 |

### 镜 2 · 亮点1 胜诉率 KPI（0:18–1:00）｜胜诉率环形图
| 项 | 内容 |
|---|---|
| **操作** | 轻点「维权胜诉率」卡（环形图动画重放）；鼠标下移到 ①品类热力 / ③供应商红黑榜入镜即可，不用切 tab |
| **预期画面** | `#kWin` 显示 **31%**、`#kWinDonut` 绿色弧约 1/3；品类热力里红色=难赢的品类 |
| **口播要点** | 每十笔只赢三笔；说到「31%」停顿 1 秒；「白亏」加重 |

### 镜 3 · 亮点2 供应商红黑榜（1:00–1:50）｜供应商透视 tab
| 项 | 内容 |
|---|---|
| **操作** | 顶部点 `[供应商透视]`（`[data-tab="supplier"]`）→ 黑榜卡片自动排前 → **点开 S6（海贸乱发）卡片** → 弹层展示 SKU 清单 / 平台分布 / 缺陷构成 → 点遮罩/✕/Esc 关闭 → 下滚到「平台×供应商 维权交叉」矩阵 |
| **预期画面** | 卡片顶部为黑榜：**S6 海贸乱发(劣) 质量分 17.8 高风险**、**S3 鑫源电子(劣) 18.6 高风险**、S8 万通杂货 22.2 待改进；点 S6 后弹层含其名下 SKU 与平台胜诉率；矩阵展示供应商×平台差异 |
| **口播要点** | "黑榜在前就是最该换的"；下钻操作放慢；矩阵处短停半秒 |

### 镜 4 · 亮点3 维度扩展看板（1:50–2:40）｜市场洞察 · ⑬⑭⑮（新增）
| 项 | 内容 |
|---|---|
| **操作** | 切回 `[市场洞察]` → 鼠标从 **⑬退货地区分布** 横移到 **⑭退货季节趋势** 再到 **⑮退货成本 & 供应商黑名单**，强调同一屏不用切页 |
| **预期画面** | ⑬ `#regionBars/#regionTbl`：欧洲 **454 笔 / ¥81,434**（胜诉率 32.2%）居首、北美 174 / ¥30,571、南美 44 / ¥8,710；⑭ `#seasonBars/#seasonTbl`：夏季 **500 笔 / ¥90,803** ≈ 春季（172 / ¥29,912）**2.9 倍**；⑮ `#costKpis/#blacklist`：退货总成本 **¥141,708**（退款 ¥120,835 + 物流 ≈¥20,873），自动列 **7 家质量分<50** 高风险供应商（S6 17.8 / S3 18.6 / S8 22.2 …）及可解释理由 |
| **口播要点** | "同一屏三层维度"；说到「黑名单自动生成」轻点 ⑮；每卡停 0.5s |

### 镜 5 · 亮点4 单案取证（2:40–3:30）｜单案取证 tab
| 项 | 内容 |
|---|---|
| **操作** | 顶部点 `[单案取证]`（`[data-tab="forensic"]`）→ 步骤条「① 上传对比图」高亮 → 拖入 `退货图.png` 与 `主图.png`，SKU/金额留默认 → 点「开始举证」→ 步骤条跳「② AI 分析」+ "正在生成取证结果…"提示条 → 约 0.5–1s 出结果 → 步骤条「③ 生成举证材料」+ 结果区顶部 **「演示模式」徽标** → 相似度进度条 → 是否同款判定 → 缺陷红框 canvas（带置信度示意）→ 「自动生成的举证材料」→ 点 **「复制」**（变"已复制"）→ 语音播放条 |
| **预期画面** | 相似度约 **78%**、判定「疑似调包/非同款」、红框框出缺陷区域（标注置信度示意）、举证材料含 SKU/相似度/瑕疵/一致性、语音可播放；步骤条全程指示当前环节 |
| **口播要点** | "上传两张图……框出问题，再生成一份能提交平台仲裁的举证材料，连给客服念的语音稿都写好了。" 红框出现时稍停；强调「一键复制，可直接粘贴提交」「置信度为示意、不替代裁决」 |
| **可选 live** | 表单「运行方式」选「真实 AI（需 Key）」→ 提示变"真实 AI 分析中（约 30-60 秒）"；结果徽标变 **「真实 AI 归因」**（绿）。**录屏建议用 mock 保节奏**，如需展示真 AI，提前 60 秒准备好再切镜头 |

### 镜 6 · 收尾 + 技术彩蛋（3:30–4:00）｜市场洞察 全景 + prompts.py
| 项 | 内容 |
|---|---|
| **操作 A** | 点 `[市场洞察]` → 大屏定格 2 秒 → 缓慢上移到「洞察报告/选品建议」区 |
| **预期画面 A** | 大屏全景 + 报告区文字（根因/建议） |
| **口播 A** | 从一笔退货的举证，到整套选品和品控的策略 |
| **操作 B（彩蛋）** | Alt+Tab 切到已打开的 `demo/prompts.py`，展示文件顶部分组注释与 `build_insights_prompt` / `consistency_prompt` 等函数名，1–2 秒后切回 |
| **口播 B** | "所有 AI 提示词都集中放在 prompts.py，按功能 ①~⑥ 分组，可审计、可调参。"（超时可直接删此镜） |

---

## 二、高光场景速查（当前 demo 源真实值，2026-08-19 核）

| 场景 | 在哪看 | 真实值（口播用它） |
|---|---|---|
| 胜诉率 KPI | 市场洞察 · 维权胜诉率卡 | **31%**（673 案） |
| 累计退款 | 市场洞察 · 累计退款卡 | **¥120,835** |
| 异常 SKU 预警 | 市场洞察 · SKU 明细表「预警」列 | **SKU-303** 近30天25笔 vs 前3笔（+733%）、**SKU-306** 29笔 vs 6笔（+383%） |
| 供应商黑榜 | 供应商透视 · 顶部卡片 | **S6 海贸乱发 17.8 高风险**、**S3 鑫源电子 18.6 高风险**、S8 万通杂货 22.2 待改进 |
| 单案取证结果 | 单案取证 · 结果区 | mock 模式：相似度约 **78%**、判定「疑似调包/非同款」、徽标「演示模式」 |
| ⑬ 地区分布 | 市场洞察 · ⑬ 卡 | 欧洲 **454笔/¥81,434**、北美 174/¥30,571、南美 44/¥8,710 |
| ⑭ 季节趋势 | 市场洞察 · ⑭ 卡 | 夏 **500笔/¥90,803** ≈ 春（172/¥29,912）**2.9 倍** |
| ⑮ 成本&黑名单 | 市场洞察 · ⑮ 卡 | 退货总成本 **¥141,708**（物流≈¥20,873）；自动黑名单 **7 家**质量分<50（S6 17.8 / S3 18.6 / S8 22.2 …） |
| 提示词集中管理 | 代码层 `demo/prompts.py` | 按功能 ①~⑥ + 编排层分组；`build_insights_prompt` / `consistency_prompt` / `dossier_prompt` / `voice_prompt` |

> 数字来源：`GET /api/insights?source=demo&mode=mock` 与 `POST /api/analyze(mock)` 实测。若更换数据源或重灌种子，请重跑一次再核（⑬⑭⑮ 尤甚）。

---

## 三、工程亮点速查（评审用，逐条可点可证）

| 亮点 | 落点 | 怎么在演示/代码里证 |
|---|---|---|
| 提示词集中管理 | `demo/prompts.py` | 所有 AI 提示词按功能 ①~⑥ + 编排层分组，单文件可审计、可调参（见镜 6 彩蛋） |
| 双源物理隔离 | `cases.db` / `cases_real.db` | 顶栏「演示数据 / 实际数据」切换；`?source=demo\|real` 透传，互不污染 |
| 写接口鉴权 | `main.py:_require_api_key` | 设 `ANALYZE_API_KEY` 后 `POST/DELETE /api/cases`、`POST /api/analyze` 强制 Key；演示态免鉴权 |
| Live/Mock 双模 | `pipeline.py` + `models_router.py` | 默认 mock 零外网；live 走百炼网关，失败自动回退 mock |
| 只取证不裁决 | 前端标注 | 代理指标提示、红框置信度"示意"、举证包只列平台客观要求 |

---

## 四、兜底与节奏

- **数据没刷新**：点一次「刷新看板」，肉眼确认胜诉率显示再进镜头。
- **单案取证找不到图**：桌面或 `demo/` 目录预置 `test_a.png`/`test_b.png`，直接拖入。
- **live 模式慢**：分析中提示条会写明"约 30-60 秒"，不要让评委误以为卡死；赶时间就用 mock。
- **口播与操作重叠**：先想好下一句再点击，不要"先做完再讲"；数字处留 0.5–1s 空镜。
- **导出**：1080p / 30fps / H.264，命名 `ReturnGuard_复赛_维度扩展版_YYYYMMDD.mp4`。

---

## 五、版本记录

| 版本 | 日期 | 说明 |
|---|---|---|
| V1.0 | 2026-08-19 | 首版：把录屏脚本 5 镜逐镜映射到真实页面路径与真实数据，含高光场景速查与兜底。 |
| V2.0 | 2026-08-19 | 维度扩展版：新增 ⑬⑭⑮ 维度卡走位（地区/季节/成本&黑名单，含真实数值）；新增镜 6 技术彩蛋映射 `demo/prompts.py` 提示词集中管理；新增「工程亮点速查」评审表。与《复赛录屏脚本_V2_维度扩展版.md》《评审一页纸.md》三件同步。 |
